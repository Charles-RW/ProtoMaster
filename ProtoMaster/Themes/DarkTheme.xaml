<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:dock="https://github.com/Dirkster99/AvalonDock">

    <!-- 基础颜色定义 -->
    <SolidColorBrush x:Key="WindowBackgroundBrush" Color="#1E1E1E"/>
    <SolidColorBrush x:Key="PanelBackgroundBrush" Color="#252526"/>
    <SolidColorBrush x:Key="SecondaryBackgroundBrush" Color="#2D2D30"/>
    <SolidColorBrush x:Key="TertiaryBackgroundBrush" Color="#333337"/>

    <SolidColorBrush x:Key="BorderBrush" Color="#3F3F46"/>
    <SolidColorBrush x:Key="SeparatorBrush" Color="#3F3F46"/>

    <SolidColorBrush x:Key="PrimaryTextBrush" Color="#CCCCCC"/>
    <SolidColorBrush x:Key="SecondaryTextBrush" Color="#9D9D9D"/>
    <SolidColorBrush x:Key="DisabledTextBrush" Color="#656565"/>

    <SolidColorBrush x:Key="AccentBrush" Color="#007ACC"/>
    <SolidColorBrush x:Key="AccentHoverBrush" Color="#1C97EA"/>
    <SolidColorBrush x:Key="SelectionBrush" Color="#094771"/>

    <SolidColorBrush x:Key="MenuBackgroundBrush" Color="#2D2D30"/>
    <SolidColorBrush x:Key="MenuItemHoverBrush" Color="#3E3E40"/>
    <SolidColorBrush x:Key="MenuBorderBrush" Color="#3F3F46"/>
    
    <SolidColorBrush x:Key="ToolBarBackgroundBrush" Color="#2D2D30"/>
    <SolidColorBrush x:Key="StatusBarBackgroundBrush" Color="#007ACC"/>
    <SolidColorBrush x:Key="StatusBarTextBrush" Color="#FFFFFF"/>

    <SolidColorBrush x:Key="TreeViewBackgroundBrush" Color="#252526"/>
    <SolidColorBrush x:Key="TreeViewItemHoverBrush" Color="#2A2D2E"/>
    <SolidColorBrush x:Key="TreeViewItemSelectedBrush" Color="#094771"/>

    <SolidColorBrush x:Key="TitleBarBackgroundBrush" Color="#3C3C3C"/>
    <SolidColorBrush x:Key="TitleBarTextBrush" Color="#FFFFFF"/>
    <SolidColorBrush x:Key="DockingManagerBackgroundBrush" Color="#2D2D30"/>

    <Style x:Key="AnchorableTitleTextStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource TitleBarTextBrush}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="FontSize" Value="12"/>
    </Style>

    <Style TargetType="{x:Type dock:LayoutAnchorableControl}">
        <Setter Property="Foreground" Value="{DynamicResource TitleBarTextBrush}"/>
    </Style>

    <!-- 修复菜单样式 -->
    <Style TargetType="Menu">
        <Setter Property="Background" Value="{DynamicResource MenuBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource MenuBorderBrush}"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
    </Style>

    <!-- 修改后的 MenuItem 模板（只列出受影响部分） -->
    <Style TargetType="MenuItem">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="MenuItem">
                    <!-- 将 Border 默认厚度设为底边 1px，但默认颜色透明，避免 hover 时导致尺寸变化 -->
                    <Border x:Name="Border" Background="{TemplateBinding Background}" 
                            BorderBrush="Transparent" BorderThickness="0,0,0,1" SnapsToDevicePixels="True">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="Icon"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="Shortcut"/>
                                <ColumnDefinition Width="13"/>
                            </Grid.ColumnDefinitions>

                            <Rectangle x:Name="HoverOverlay" Fill="{DynamicResource MenuItemHoverOverlayBrush}" Opacity="0" IsHitTestVisible="False"/>

                            <!-- 第0列：对号区域 + Icon -->
                            <Grid Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Center" MinWidth="18">
                                <Viewbox Width="12" Height="12" HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <Canvas Width="12" Height="12">
                                        <Path x:Name="CheckMark"
                                              Data="M2,7 L5,10 L10,3"
                                              Stroke="{DynamicResource PrimaryTextBrush}"
                                              StrokeThickness="2"
                                              StrokeStartLineCap="Round"
                                              StrokeEndLineCap="Round"
                                              Visibility="Collapsed"/>
                                    </Canvas>
                                </Viewbox>

                                <ContentPresenter x:Name="Icon"
                                                  ContentSource="Icon"
                                                  Margin="6,0,6,0"
                                                  VerticalAlignment="Center"
                                                  HorizontalAlignment="Center"/>
                            </Grid>

                            <ContentPresenter x:Name="HeaderHost" Grid.Column="1"
                                              ContentSource="Header"
                                              RecognizesAccessKey="True"
                                              VerticalAlignment="Center"/>

                            <TextBlock x:Name="InputGestureText" Grid.Column="2"
                                       Text="{TemplateBinding InputGestureText}"
                                       Margin="5,2,2,2"
                                       Foreground="{DynamicResource SecondaryTextBrush}"/>

                            <Path x:Name="ArrowPath" Grid.Column="3"
                                  Data="M 0 0 L 0 7 L 4 3.5 Z"
                                  Fill="{DynamicResource PrimaryTextBrush}"
                                  Margin="4,0,6,0"
                                  VerticalAlignment="Center"/>
                            <!-- Popup 必须放在 Grid 内容的最后 -->
                            <Popup x:Name="Popup" Placement="Right"
                                   IsOpen="{TemplateBinding IsSubmenuOpen}"
                                   AllowsTransparency="True" Focusable="False"
                                   PopupAnimation="Fade">
                                <Border Background="{DynamicResource MenuBackgroundBrush}"
                                        BorderBrush="{DynamicResource BorderBrush}"
                                        BorderThickness="1">
                                    <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle"/>
                                </Border>
                            </Popup>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="Role" Value="TopLevelHeader">
                            <Setter Property="Grid.IsSharedSizeScope" Value="True"/>
                            <Setter Property="Padding" Value="6,1,6,1"/>
                            <Setter TargetName="Popup" Property="Placement" Value="Bottom"/>
                            <Setter TargetName="ArrowPath" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                        
                        <Trigger Property="Role" Value="TopLevelItem">
                            <Setter Property="Padding" Value="6,1,6,1"/>
                            <Setter TargetName="ArrowPath" Property="Visibility" Value="Collapsed"/>
                        </Trigger>

                        <Trigger Property="Role" Value="SubmenuHeader">
                            <Setter Property="DockPanel.Dock" Value="Top"/>
                            <Setter Property="Padding" Value="0,2,0,2"/>
                        </Trigger>

                        <Trigger Property="Role" Value="SubmenuItem">
                            <Setter Property="DockPanel.Dock" Value="Top"/>
                            <Setter Property="Padding" Value="0,2,0,2"/>
                            <Setter TargetName="ArrowPath" Property="Visibility" Value="Collapsed"/>
                        </Trigger>

                        <Trigger Property="IsSuspendingPopupAnimation" Value="True">
                            <Setter TargetName="Popup" Property="PopupAnimation" Value="None"/>
                        </Trigger>

                        <Trigger Property="Icon" Value="{x:Null}">
                            <Setter TargetName="Icon" Property="Visibility" Value="Collapsed"/>
                        </Trigger>

                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{DynamicResource MenuItemHoverBrush}"/>
                            <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource MenuItemHoverBorderBrush}"/>
                            <!-- 不改变 BorderThickness，以防止尺寸变化 -->
                            <Setter TargetName="HoverOverlay" Property="Opacity" Value="1"/>
                            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
                        </Trigger>

                        <!-- IsChecked 触发同时显示对号 -->
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                            <!-- 仍保持底边 1px，避免尺寸抖动 -->
                            <Setter TargetName="HoverOverlay" Property="Opacity" Value="1"/>
                            <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="CheckMark" Property="Stroke" Value="{DynamicResource AccentBrush}"/>
                        </Trigger>

                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{DynamicResource DisabledTextBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- AvalonDock 相关样式 -->
    <Style TargetType="{x:Type dock:DockingManager}">
        <Setter Property="Background" Value="{DynamicResource DockingManagerBackgroundBrush}"/>
    </Style>

    <!-- AvalonDock 锚点面板标题栏样式 -->
    <Style x:Key="AvalonDockTitleStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource TitleBarTextBrush}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Margin" Value="4,2"/>
    </Style>

    <!-- 其他现有样式保持不变 -->
    <Style x:Key="ThemedMenu" TargetType="Menu">
        <Setter Property="Background" Value="{DynamicResource MenuBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
    </Style>

    <Style x:Key="ThemedMenuItem" TargetType="MenuItem">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
        <Style.Triggers>
            <Trigger Property="IsHighlighted" Value="True">
                <Setter Property="Background" Value="{DynamicResource MenuItemHoverBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="ThemedToolBar" TargetType="ToolBar">
        <Setter Property="Background" Value="{DynamicResource ToolBarBackgroundBrush}"/>
    </Style>

    <Style x:Key="ThemedButton" TargetType="Button">
        <Setter Property="Background" Value="{DynamicResource SecondaryBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="8,4"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}" 
                            BorderBrush="{TemplateBinding BorderBrush}" 
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{DynamicResource TertiaryBackgroundBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ThemedTextBlock" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
    </Style>

    <Style x:Key="SecondaryTextBlock" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource SecondaryTextBrush}"/>
    </Style>

    <Style x:Key="ThemedPanel" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource PanelBackgroundBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>

    <Style x:Key="ThemedTitleBar" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource TitleBarBackgroundBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
    </Style>

    <Style x:Key="ThemedTreeView" TargetType="TreeView">
        <Setter Property="Background" Value="{DynamicResource TreeViewBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
        <Setter Property="BorderThickness" Value="0"/>
    </Style>

    <Style x:Key="ThemedTreeViewItem" TargetType="TreeViewItem">
        <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
        <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}"/>
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{DynamicResource TreeViewItemSelectedBrush}"/>
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource TreeViewItemHoverBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="ThemedStatusBar" TargetType="StatusBar">
        <Setter Property="Background" Value="{DynamicResource StatusBarBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{DynamicResource StatusBarTextBrush}"/>
    </Style>

    <Style x:Key="ThemedGridSplitter" TargetType="GridSplitter">
        <Setter Property="Background" Value="{DynamicResource BorderBrush}"/>
    </Style>

    <!-- 新增菜单项悬停边框和覆盖颜色 -->
    <SolidColorBrush x:Key="MenuItemHoverBorderBrush" Color="#FF007ACC"/>
    <SolidColorBrush x:Key="MenuItemHoverOverlayBrush" Color="#22007ACC"/>

</ResourceDictionary>